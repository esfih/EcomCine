# master-core

Reusable, stack-agnostic development foundation for app-local product repositories.

This repository owns:

- IDE AI context and workflow rules
- reusable feature-package discipline
- generic validation and handover scripts
- shared development conventions that are not WordPress-specific

App-local repos should import this repository into `foundation/core` via subtree.